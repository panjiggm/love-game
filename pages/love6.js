import { useState } from "react";
import Link from "next/link";
import Head from "next/head";
import styles from "../styles/Home.module.css";

const Love6 = () => {
  let answer = "172";

  const [like, setLike] = useState(null);
  const [isTrue, setIsTrue] = useState(false);

  const handleQuestion = (e) => {
    const val = e.target.value.toLowerCase();

    if (val === answer) {
      setIsTrue(true);
    } else {
      setIsTrue(false);
    }
  };

  return (
    <div className={styles.container}>
      <Head>
        <title>Love 6 | Weird Game</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>#6</h1>
        <br />
        <br />

        <h2 className={styles.quotes}>
          “ Kamu tidak butuh jarak ribuan kilometer untuk merindukanku. Karena
          aku juga begitu.”
        </h2>
        <br />

        <div className="row">
          <button
            className="col col-md-4 btn btn-danger"
            onClick={() => setLike(false)}>
            👎
          </button>
          <button className="col col-md-4" style={{ opacity: 0 }}></button>
          <button
            className="col col-md-4 btn btn-success"
            onClick={() => setLike(true)}>
            👍
          </button>
        </div>

        <div>
          {!like && like !== null && (
            <>
              <br />
              <p className="text-danger">
                Kamu ga suka ungkapan perasaanku 😔 ?
              </p>
            </>
          )}
        </div>

        <div>
          {like && like !== null && (
            <>
              <br />
              <br />
              <p className={styles.title}>
                Berapa tinggi badan orang yang kamu rindukan?
              </p>
              <h5
                className="text-center"
                onClick={() =>
                  alert("Hmm kamu beneran ga tau 😢 ? clue : 170 - 175")
                }>
                <u>clue</u>
              </h5>
              <br />
              <input
                className="form-control"
                onChange={handleQuestion}
                placeholder="awas sih kalo lupa :D"
              />
              <br />
              {isTrue && (
                <Link href="/love7">
                  <button type="button" className="btn btn-primary">
                    Jawaban kamu benar! lanjut
                  </button>
                </Link>
              )}
            </>
          )}
        </div>
      </main>

      <footer className={styles.footer}>
        <p className={styles.footerTxt}>dibuat dengan ❤️ oleh @panjiggm</p>
      </footer>
    </div>
  );
};

export default Love6;
